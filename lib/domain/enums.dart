
enum AppointmentStatus { scheduled, completed, cancelled, noShow }

enum TriageLevel { red, yellow, green } 

enum EmergencyStatus { active, stable, discharged, critical } 

enum BillStatus { draft, issued, paid, overdue, cancelled } 

enum UserRole { admin, doctor, nurse, receptionist, patient } 

enum EquipmentStatus { operational, maintenance, broken } 

enum WardType { icu, pediatrics, surgery, maternity, general } 
