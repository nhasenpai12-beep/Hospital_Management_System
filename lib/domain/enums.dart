
enum AppointmentStatus { scheduled, completed, cancelled, noShow }

enum TriageLevel { red, yellow, green } 

enum EmergencyStatus { active, stable, discharged, critical } // NEW

enum BillStatus { draft, issued, paid, overdue, cancelled } // NEW

enum UserRole { admin, doctor, nurse, receptionist, patient } // NEW

enum EquipmentStatus { operational, maintenance, broken } // NEW

enum WardType { icu, pediatrics, surgery, maternity, general } // NEW
